CREATE OR REPLACE PROCEDURE GetEmployeeDetails(p_EmployeeID NUMBER) IS
BEGIN
    SELECT * FROM Employee WHERE EmployeeID = p_EmployeeID;
END GetEmployeeDetails;
/
EXECUTE GetEmployeeDetails(3); -- Replace 3 with the desired Employee ID
