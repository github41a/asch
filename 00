Yes, you can specify a start month and year for your partitions and then provide an end date for the last partition. Here's an example:

Assuming you want to partition a table named "sales_data" on the "order_date" column from January 2020 to December 2025, you can use the following SQL script:

```sql
-- Define the initial partition
ALTER TABLE sales_data
ADD PARTITION (PARTITION p202001 VALUES LESS THAN (TO_DATE('2020-02-01', 'YYYY-MM-DD')));

-- Generate partitions for each month until December 2025
BEGIN
  FOR year_month IN 202002..20251201 LOOP
    EXECUTE IMMEDIATE 'ALTER TABLE sales_data ADD PARTITION (PARTITION p' || TO_CHAR(year_month) || ' VALUES LESS THAN (TO_DATE(''' || TO_CHAR(year_month + 1) || '-01'', ''YYYYMM''))';
  END LOOP;
END;
/

-- Define the final partition
ALTER TABLE sales_data
ADD PARTITION (PARTITION p202601 VALUES LESS THAN (TO_DATE('2026-01-01', 'YYYY-MM-DD')));
```

In this example, the script creates monthly partitions from February 2020 to December 2025 and ends with a partition for January 2026. You can adjust the start and end dates to meet your specific requirements.
